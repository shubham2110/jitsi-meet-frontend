/**
 * The (name of the) command which transports the state (represented by
 * {State} for the local state at the time of this writing) of a {ForcedMute}
 * (instance) between participants.
 */
export const FORCED_MUTE_COMMAND = 'forced-mute';
